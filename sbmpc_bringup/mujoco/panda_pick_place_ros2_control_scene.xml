<mujoco model="panda_ros2_control pick and place">
  <include file="panda_ros2_control.xml"/>

  <statistic center="0.3 0 0.4" extent="1"/>

  <option timestep="0.001" iterations="5" ls_iterations="8" integrator="implicitfast">
    <flag eulerdamp="disable"/>
  </option>

  <custom>
    <numeric data="12" name="max_contact_points"/>
  </custom>

  <visual>
    <headlight diffuse="0.6 0.6 0.6" ambient="0.3 0.3 0.3" specular="0 0 0"/>
    <rgba haze="0.15 0.25 0.35 1"/>
    <global azimuth="120" elevation="-20"/>
    <scale contactwidth="0.075" contactheight="0.025" forcewidth="0.05"
     com="0.05" framewidth="0.01" framelength="0.2"/>
  </visual>

  <asset>
    <texture type="skybox" builtin="gradient" rgb1="0.3 0.5 0.7"
     rgb2="0 0 0" width="512" height="3072"/>
    <texture type="2d" name="groundplane" builtin="checker" mark="edge"
     rgb1="0.2 0.3 0.4" rgb2="0.1 0.2 0.3" markrgb="0.8 0.8 0.8"
     width="300" height="300"/>
    <material name="groundplane" texture="groundplane" texuniform="true"
     texrepeat="5 5" reflectance="0.2"/>
  </asset>

  <worldbody>
    <light pos="0 0 1.5" dir="0 0 -1" directional="true"/>
    <geom name="floor" size="0 0 0.05" type="plane" material="groundplane"
     contype="1" conaffinity="1"/>

    <!-- Object to pick up -->
    <body name="object" pos="0.5 0.0 0.025">
      <freejoint name="object_joint"/>
      <geom type="box" name="object_geom" size="0.02 0.02 0.025"
       mass="0.05" condim="4"
       friction="1.0 0.03 0.003" rgba="0.2 0.6 0.2 1"
       contype="2" conaffinity="1"
       solref="0.01 1" solimp="0.99 0.995 0.01"/>
    </body>

    <!-- Target placement position (mocap, user-draggable) -->
    <body mocap="true" name="target" pos="0.5 0.3 0.15">
      <geom type="box" size="0.02 0.02 0.025" rgba="1 0.2 0.2 0.3"
       contype="0" conaffinity="0"/>
    </body>
  </worldbody>

  <!-- Sensors for the task cost computation -->
  <sensor>
    <!-- End-effector (gripper) position in world frame -->
    <framepos name="ee_pos" objtype="site" objname="gripper"/>
    <!-- End-effector rotation matrix (9 values, row-major) -->
    <framexaxis name="ee_xaxis" objtype="site" objname="gripper"/>
    <frameyaxis name="ee_yaxis" objtype="site" objname="gripper"/>
    <framezaxis name="ee_zaxis" objtype="site" objname="gripper"/>
    <!-- Object position in world frame -->
    <framepos name="obj_pos" objtype="body" objname="object"/>
    <!-- Target (mocap) position in world frame -->
    <framepos name="target_pos" objtype="body" objname="target"/>
  </sensor>

  <keyframe>
    <!-- Neutral / folded config: EE points straight down -->
    <key name="home"
      qpos="0 -0.785 0 -2.356 0 1.571 0.785 0.04 0.04
            0.5 0.0 0.025 1 0 0 0"
      ctrl="0 -0.785 0 -2.356 0 1.571 0.785 0.04"/>
  </keyframe>
</mujoco>
